# Checkpoint 35: Onboarding Density Fix

Date: 2026-04-17
Branch: `design-2`

## What changed

- Reduced the oversized onboarding headline and replaced long sentence headings with short product headings.
- Removed the duplicate step-chip row from the visible layout so the top segmented progress bar carries progress instead.
- Shortened welcome copy and converted the three cramped feature cards into cleaner scan rows.
- Reduced the right-side preview illustration/card scale and hid preview summary cards on the welcome step.
- Kept the UX Pilot direction, but made the screen calmer and less congested.

## Verification

- `npm run build` passed in `dashboard/`
- `npm run lint` passed in `dashboard/`
